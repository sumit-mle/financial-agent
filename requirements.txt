# =============================================================================
# Fin AI Agent — Python dependencies (Python 3.11)
# Strategy: pin exact versions only where stability is critical (LLM stack).
# Use >= minimums for support libraries to resolve transitive conflicts.
# =============================================================================

# ── Orchestration ─────────────────────────────────────────────────────────────
langgraph==0.2.28
langchain==0.3.7
langchain-core==0.3.15
langchain-openai==0.2.5
langchain-community==0.3.5
langchain-text-splitters==0.3.2

# ── LLM providers ────────────────────────────────────────────────────────────
openai==1.54.0
anthropic==0.36.2

# ── Embeddings & Vector Store ─────────────────────────────────────────────────
qdrant-client==1.11.3
# sentence-transformers brings torch (~600MB). Only install for local embedding.
# Default provider is OpenAI (set LLM_PROVIDER=openai + OPENAI_API_KEY).
# Uncomment the line below only if you set LLM_PROVIDER=local:
# sentence-transformers>=2.7.0,<4.0.0
tiktoken==0.8.0

# ── Reranking ─────────────────────────────────────────────────────────────────
# flashrank pulls onnxruntime (~200MB). Installed lazily at runtime on first use.
# flashrank==0.2.9
cohere>=5.0.0,<6.0.0

# ── Document ingestion ────────────────────────────────────────────────────────
pypdf>=5.0.0,<6.0.0
pandas>=2.0.0,<3.0.0

# ── API layer ─────────────────────────────────────────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.32.0
gunicorn==23.0.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12
httpx==0.27.2

# ── Observability ─────────────────────────────────────────────────────────────
langfuse==2.53.3
prometheus-client==0.21.1

# NOTE: ragas is not installed in the main image — it pulls heavy ML deps
# and conflicts with sqlalchemy pins. Install separately for eval jobs:
#   pip install ragas datasets
# ragas==0.2.6

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0
asyncpg>=0.29.0,<1.0.0
alembic>=1.13.0,<2.0.0
redis>=5.0.0,<6.0.0

# ── Auth & Security ────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity>=8.0.0,<10.0.0
python-dotenv>=1.0.0,<2.0.0
structlog>=24.0.0,<25.0.0
rich>=13.0.0,<14.0.0
typer>=0.9.0,<1.0.0
# ── Specialized Models (Layer 4 - Task-Specific Models) ──────────────────────
# These dependencies match the architecture diagram's specialized model requirements

# PII Detection Model (Advanced NER-based)
presidio-analyzer>=2.2.0,<3.0.0
presidio-anonymizer>=2.2.0,<3.0.0
spacy>=3.7.0,<4.0.0

# Sentiment Analysis Model (Financial domain-specific)
transformers>=4.35.0,<5.0.0
torch>=2.1.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Classification Models (Product/Policy routing)
scikit-learn>=1.3.0,<2.0.0

# Learn & Improve Pipeline (Layer 6)
# Note: These are for automated feedback analysis and quality monitoring
# Used by the feedback aggregator and quality monitoring systems
# ── Monitoring & Observability ────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0

# ── External Integrations ──────────────────────────────────────────────────────
simple-salesforce==1.12.6
requests-oauthlib==2.0.0

# ── Testing & Development ──────────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.8
pytest-mock==3.14.0
pytest-cov==4.1.0
pytest-xdist==3.6.0
httpx[test]==0.27.2
factory-boy==3.3.1

# ── Statistical Analysis ───────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.14.1
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0